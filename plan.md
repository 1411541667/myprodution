# 悠哉的小屋：项目计划与当前实现交接

> 最后更新：2026-09-18  
> 工作区：`D:\MyProduction`  
> 项目阶段：本地内容工作流已完善（含备份、图片压缩、排序与详情预览），等待录入真实内容或迁移至服务器

## 1. 文档用途

本文档用于让后续开发者或更换后的模型快速理解：

- 用户最初想要什么；
- 当前版本已经实现了什么；
- 代码、数据和页面如何组织；
- 哪些功能只是本地原型，不具备生产安全性；
- 后续修改应从哪里开始，以及应保持哪些产品约束。

接手修改前，应先阅读本文档、`README.md`、`src/types.ts`，然后根据任务检查对应组件。不要假设已经存在服务器、数据库或云端资源。

## 2. 产品目标

制作一个 React + Vite 个人作品集网站，整体气质为“悠哉的小屋”：温暖、松弛、带一点 QQ 空间式的个人生活感，同时用小红书式多列排版展示项目。

完整访客路径：

1. 进入纯白开始页面；
2. 点击“进入悠哉的小屋”；
3. 进入个人主页，浏览个人资料、生活动态和项目；
4. 搜索、筛选或收藏项目；
5. 点击项目卡片，在手机屏幕形态的弹层内滚动鉴赏图文详情；
6. 可以在留言簿提交留言，留言需管理员审核后才公开。

管理员不应在游客界面看到公开入口。管理员通过私密地址 `/studio` 和管理口令进入后台，编辑个人资料、动态、项目和留言。

## 3. 已确认的产品决策

- 技术栈：React、TypeScript、Vite、原生 CSS、Lucide 图标。
- 当前阶段：本地优先，不依赖服务器即可完整演示。
- 初始数据：完全空白，不内置虚构个人信息或示例项目。
- 视觉方向：暖白色为默认主题，融合 QQ 空间个人主页结构与小红书项目瀑布流。
- 入场页：通体纯白，中心仅有“进入悠哉的小屋”，带轻微呼吸与淡出动画。
- 个人区域：资料卡、说说和时间线；个人内容与项目内容明确分开。
- 项目详情：图文长页、详情图片和外部链接，置于可滚动手机框中。
- 图片：后台选择本地图片后可预览并保存在浏览器中。
- 收藏：仅保存在当前游客浏览器，不做账号同步。
- 主题：游客自行选择暖白、青绿或夜色，默认暖白。
- 留言：一律先进入待审核列表，由管理员人工决定公开、隐藏或删除。
- AI 审核：本地版本不实现；未来必须通过服务端代理接入，禁止在前端保存模型 API 密钥。
- 管理入口：不出现在游客导航或页面内容中，仅通过 `/studio` 访问。

## 4. 当前已实现内容

### 4.1 游客端

- 纯白入场页、呼吸动画和淡出转场。
- 导航：关于我、生活切片、我的作品、留言簿。
- 个人封面、头像、名称、账号、简介、地点和个人标签。
- 说说与时间线列表。
- 项目瀑布流，自适应桌面与手机屏幕。
- 项目关键词搜索、标签筛选和“只看收藏”。
- 当前浏览器内收藏与取消收藏。
- 暖白、青绿、夜色三套主题。
- 项目卡片三种展示比例：故事封面、画廊大图、极简卡片。
- 手机屏幕式项目详情弹层，可独立滚动。
- 详情支持封面、标签、标题、摘要、文字块、图片块、图片说明和外部链接。
- 留言提交与已公开留言展示。
- 全站空内容状态。
- `prefers-reduced-motion` 动效降级支持。
- 损坏或手改过的浏览器本地偏好（主题、收藏）不会导致白屏，非法值会回退到默认。
- 项目详情弹层支持 `Esc` 关闭、自动聚焦关闭按钮，并在打开期间锁定页面滚动。

### 4.2 管理后台

- `/studio` 管理口令登录及当前标签页会话保持。
- 总览统计、最近项目和内容完整度提示。
- 个人资料编辑与头像、封面上传，每个图片字段可单独移除图片。
- 动态新增、编辑、上下移排序与删除。
- 项目列表、编辑、删除、发布、取消发布、草稿状态与上下移排序。
- 三步快捷上传向导：
  1. 基本信息、封面、标签和卡片模板；
  2. 详情文字块、图片块（可上下移排序）与带协议校验的外部链接；
  3. 卡片预览、手机框内的完整详情预览、完整度检查和发布。
- 发布校验：标题必填、空内容块保存时自动跳过、外部链接只允许 `http`/`https`。
- 编辑已发布项目时，「保存修改」不再意外取消发布；取消发布是独立按钮。
- 留言待审、公开、重新隐藏和删除。
- 本地内容概览、JSON 备份导出与导入（导入前显示条目数量并要求确认）。
- 清空全部内容前提供一键导出备份与二次确认。
- 保存与图片处理失败都会给出中文错误提示，不会静默失败。

## 5. 页面与代码结构

```text
D:\MyProduction
├─ index.html                  Vite HTML 入口
├─ package.json               依赖和启动、构建命令
├─ vite.config.ts             Vite 配置，本地端口 5173
├─ README.md                  最短启动说明
├─ plan.md                    本交接文档
└─ src
   ├─ main.tsx                React 挂载入口
   ├─ App.tsx                 加载数据、串行保存、按路径分发游客端或后台
   ├─ types.ts                全部核心数据类型与状态更新签名
   ├─ data.ts                 IndexedDB 仓库、数据校验、图片处理、导入导出
   ├─ storage.ts              localStorage / sessionStorage 安全读写
   ├─ styles.css              游客端、后台、响应式和动画样式
   └─ components
      ├─ Portfolio.tsx        游客端全部页面与交互
      ├─ Admin.tsx            登录、后台管理与项目上传向导
      └─ ProjectDetail.tsx    访客详情与后台预览共用的正文渲染
```

当前没有使用 React Router。`App.tsx` 直接检查 `window.location.pathname`：路径为 `/studio` 或以 `/studio/` 开头时显示后台，其余路径显示游客端（`/studio-public` 之类不会再被误判为后台）。若未来页面数量增加，再引入路由库并保持 `/studio` 兼容。

`ProjectDetail.tsx` 同时给访客弹层和后台第 3 步预览使用，保证「后台预览 = 访客所见」；它还会过滤掉没有内容的详情块。

## 6. 数据模型

核心类型定义在 `src/types.ts`。

### Profile

个人资料：

- `name`：显示名称；
- `handle`：个性账号；
- `bio`：个人介绍；
- `location`：地点；
- `avatar`：头像 Data URL；
- `cover`：封面 Data URL；
- `tags`：个人标签数组。

### Moment

生活动态：

- `id`：UUID；
- `content`：动态正文；
- `date`：展示日期。

### Project

项目内容：

- `id`：UUID；
- `title`、`summary`、`cover`、`tags`：卡片与详情头部内容；
- `template`：`story | gallery | minimal`；
- `link`、`linkLabel`：外部链接与按钮文字；
- `blocks`：按顺序渲染的详情内容块；
- `published`：是否对游客可见；
- `createdAt`：创建日期。

详情块 `DetailBlock` 当前支持：

- `text`：文字段落；
- `image`：图片及可选图片说明。

### GuestbookEntry

留言：

- `nickname`、`content`、`createdAt`；
- `status`：`pending | published`。

只有 `published` 留言会出现在游客端。

### SiteState

统一站点状态：

```ts
interface SiteState {
  profile: Profile
  moments: Moment[]
  projects: Project[]
  messages: GuestbookEntry[]
}
```

后续接入 API 时应保留这一领域结构，优先替换数据仓库，不要把网络请求散落到展示组件中。

`src/types.ts` 另有两个面向组件的类型：

- `StateUpdater`：`(previous: SiteState) => SiteState`；
- `StateChangeHandler`：`(next: SiteState | StateUpdater) => Promise<void>`，后台表单统一用它提交改动，避免连续操作读到过期的 state 快照。

## 7. 本地存储设计

- 站点内容和上传图片：IndexedDB。
  - 数据库名：`youzai-portfolio`；
  - 对象仓库：`content`；
  - 状态键：`site-state`。
- 游客主题：localStorage 键 `youzai-theme`（非法值回退 `cream`）。
- 游客收藏：localStorage 键 `youzai-favorites`（非数组或损坏时视作空）。
- 最近一次导出备份时间：localStorage 键 `youzai-last-backup`。
- 已通过入场页：sessionStorage 键 `youzai-entered`。
- 后台当前会话：sessionStorage 键 `youzai-admin`。

`src/storage.ts` 统一封装这些读写，并对隐私模式或损坏数据做兜底。

**写入管线**：`App.tsx` 的 `updateState` 先更新界面，再把 IndexedDB 写入串行排队（`queue`），因此连续操作不会出现后写的旧数据覆盖新数据；写入失败会抛出给调用方，并在页面顶部显示常驻横幅提醒内容尚未落盘。

**读取校验**：`src/data.ts` 的 `normalizeState` 对 IndexedDB 读出的数据、导入的 JSON 做同一套清洗：缺失字段补默认、非字符串/非法联合类型回退、缺少必要内容的条目丢弃。这样旧版本数据或手工改坏的数据不会让页面崩溃。

清除浏览器站点数据会删除所有内容和图片。不同浏览器、设备或不同域名之间不会同步。图片以 Data URL 形式保存，适合原型和少量图片，不适合大型媒体库。

## 7.1 备份文件格式

`exportState` 生成的 JSON 结构：

```json
{
  "format": "youzai-portfolio",
  "version": 1,
  "exportedAt": "2026-09-18T00:00:00.000Z",
  "counts": { "projects": 0, "moments": 0, "messages": 0 },
  "state": { "profile": {}, "moments": [], "projects": [], "messages": [] }
}
```

`importState` 同时兼容带外层信封的文件和裸 `SiteState`。增加或调整字段时应提升 `BACKUP_VERSION` 并在 `normalizeState` 中保留旧字段的迁移分支。

## 7.2 图片处理限制

在 `src/data.ts` 中集中定义：

- 允许类型：`image/png`、`image/jpeg`、`image/webp`；
- 原文件上限：8 MB；
- 最长边上限：2000px，超出自动等比缩小；
- 除 PNG 外的格式统一转成质量 0.86 的 WEBP。

校验失败会抛出中文错误，由 `Admin.tsx` 的 `ImageField` 显示在图片框下方，并保留原有图片不变。

## 8. 本地运行与入口

```powershell
npm install
npm run dev
```

- 游客页面：`http://127.0.0.1:5173/`
- 管理后台：`http://127.0.0.1:5173/studio`
- 当前初始管理口令：`youzai2026`
- 口令位置：`src/components/Admin.tsx` 中的 `ADMIN_PASSWORD`

构建与预览：

```powershell
npm run build
npm run preview
```

## 9. 当前验证结果

2026-09-17 已完成以下验证：

- `npm run build` 成功；
- Vite 生产构建成功，无 TypeScript 错误；
- 入场页能正确进入主页；
- 初始空状态正常；
- `/studio` 可用口令登录；
- 项目快捷上传可以从基本信息进入详情编排步骤；
- 390px 手机宽度无横向页面溢出；
- 手机宽度会显示移动导航按钮。

2026-09-18 新增验证（Playwright + Chromium，37/37 项通过）：

- 入场页 → 首页、`/studio` 口令登录（错误口令被拒绝）；
- 2600×1400 PNG 上传后被压缩成 Data URL，最长边 ≤ 2000px；
- 个人信息保存提示、动态新增两条、上移排序、编辑生效；
- 向导在标题为空时无法跳到第 2 步，填写后可跳；
- 详情图片压缩成功、`javascript:` 链接被判非法并禁用发布；
- 第 3 步手机框内能渲染真实详情文字；
- 发布后项目列表显示「已发布」；
- 导出备份：`format = youzai-portfolio`、保留已发布项目、剥离非法链接、跳过空内容块；
- 访客端能看到姓名、动态与项目卡片，弹层详情文字与外部链接正确；
- `Esc` 可关闭弹层；
- 手动写坏 `youzai-favorites` / `youzai-theme` 后刷新不白屏，主题回退 `cream`；
- 390px 宽度横向溢出为 0，移动导航按钮可见；
- 导入备份：先出现确认面板，确认后项目被还原；导入损坏 JSON 会报错且不改动数据；
- 生产构建（`npm run preview`）入场页、首页、后台登录均正常。

唯一失败的网络请求是 `fonts.googleapis.com` 字体（当前环境无外网），与代码无关，离线时会回退系统字体。

后续每次修改至少重新执行 `npm run build`。涉及布局、动画、上传、弹层、筛选或响应式的修改，还应使用真实浏览器验证桌面和手机宽度。

## 10. 已知限制与安全边界

1. **后台口令不安全**：口令写在前端源码里，只能隐藏入口和防止误操作，不能抵御有意访问。公开部署前必须迁移到服务端鉴权。
2. **没有服务器与数据库**：内容仅存在当前浏览器；更换设备、浏览器或域名后不会自动出现。目前靠 JSON 导出/导入手动迁移。
3. **留言不是公网留言系统**：其他访客提交的数据不会同步到管理员设备，只有在同一浏览器/同一站点存储中才能看到。
4. **没有真实 AI 审核**：目前完全由管理员审核。未来 AI 只能在服务器端调用。
5. **图片仍以 Data URL 存储**：已做格式、体积与尺寸限制和压缩，但没有对象存储、上传进度或后端处理；大量图片仍会占满 IndexedDB 配额。
6. **删除与清空仍不可撤销**：没有回收站或自动备份，只能依赖用户自己导出的 JSON 备份。
7. **排序方式是上移/下移按钮**：详情块和项目已支持顺序调整，但没有拖拽交互。
8. **项目排序是手工顺序**：数组顺序即前台顺序，不按 `createdAt` 自动排序。
9. **外部字体依赖网络**：`styles.css` 通过 Google Fonts 引入字体；离线时会回退到系统字体。
10. **无自动化测试套件**：当前依靠 TypeScript 构建、浏览器脚本验证与人工检查；`package.json` 的依赖仍写成 `latest`，也没有提交锁文件。

## 11. 推荐后续路线

### 第一阶段：完善本地内容工作流（已完成）

- ✅ JSON 数据导出和导入，导入前显示条目数量并要求确认。
- ✅ 上传图片前进行尺寸限制、压缩和格式校验。
- ✅ 详情块排序、动态编辑、项目排序与详情预览。
- ✅ 删除/清空操作增加二次确认，清空前提供一键导出备份。
- ⏳ 组件拆分只完成了共享详情渲染（`ProjectDetail.tsx`）；`Portfolio.tsx`、`Admin.tsx` 仍各是一个大文件，可继续按 Tab 拆分。

### 第一阶段收尾（建议的下一次迭代）

- 把 `Admin.tsx` 按 Tab 拆成 `admin/*.tsx`，把 `Portfolio.tsx` 拆出 `Guestbook`、`ProjectCard` 等独立文件。
- 引入 Vitest，为 `normalizeState`、`importState`、`safeLink`、`moveItem` 和保存队列写单元测试。
- 锁定依赖版本并提交 `package-lock.json`，替换 `latest`。
- 增加回收站或「删除后 10 秒内可撤销」机制。
- 给详情块和项目补拖拽排序，保留现有按钮作为无障碍替代。

### 第二阶段：服务器化

- 建立正式 API、数据库与对象存储。
- 将 `loadState`、`saveState` 替换为异步 API 仓库实现（保持 `SiteState` 语义不变）。
- 后台使用服务端会话、密码哈希、CSRF 防护和访问速率限制。
- 游客留言写入服务器，后台审核状态由数据库同步。
- 图片改为服务端上传并返回 URL，不再将大图存为 Data URL。
- 增加环境变量与开发、预发布、生产配置。

### 第三阶段：AI 审核与上线

- 留言提交后由服务端调用模型进行风险分类；模型只提供审核建议，不直接公开留言。
- 保留“AI 初审 → 管理员最终决定”的状态与审计记录。
- 增加垃圾留言频率限制、敏感输入处理和管理员操作日志。
- 配置域名、HTTPS、备份、错误监控、隐私说明和部署流水线。

## 12. 后续模型的修改规则

后续模型处理本项目时应遵循：

1. 先读取本文件以及任务涉及的实际源码，不只根据旧对话推断。
2. 保持游客端看不到后台入口；除非用户明确改变需求，不要在导航中加入 `/studio`。
3. 保持个人信息、生活动态和项目内容在数据与界面上的区分。
4. 保持“暖白默认、游客可切主题”的行为。
5. 保持项目详情为手机屏幕式独立滚动区域，避免整页弹层无法关闭。
6. 留言默认必须是 `pending`，不得未经管理员决定自动公开。
7. 接入 AI 时不得把 API 密钥、系统提示或管理凭证写入浏览器代码。
8. 接入后端时优先替换 `src/data.ts` 的仓库层，并尽量保持组件接收的 `SiteState` 语义稳定。
9. 修改现有数据字段时必须考虑 IndexedDB 中旧数据的兼容或迁移，不能只改 TypeScript 类型。
10. 不要自动填充虚构的个人资料或作品；项目默认保持空白，除非用户明确要求示例数据。
11. 保留响应式支持与减少动画偏好；新增视觉效果不得破坏 390px 手机宽度。
12. 完成改动后运行 `npm run build`，并对受影响的页面路径进行浏览器验证。
13. 后台写入一律通过 `StateChangeHandler`（可传函数式更新），不要用渲染快照直接构造新状态，否则连续操作会丢失更新。
14. 图片或数据校验失败必须给用户看到中文原因，不允许静默吞掉异常或假装保存成功。
15. 访客能看到的内容必须与后台第 3 步预览一致：改详情渲染时同时改 `ProjectDetail.tsx`，不要只改其中一处。

## 13. 常见修改定位

| 修改目标 | 首要文件 |
| --- | --- |
| 个人主页、项目卡片、筛选、收藏、留言表单 | `src/components/Portfolio.tsx` |
| 后台表单、项目向导、审核流程、后台口令 | `src/components/Admin.tsx` |
| 访客详情与后台预览的正文渲染 | `src/components/ProjectDetail.tsx` |
| 配色、排版、响应式、手机框样式 | `src/styles.css` |
| 新增或修改数据字段、导入导出格式、图片限制 | `src/types.ts`、`src/data.ts` |
| 主题/收藏等浏览器偏好读写 | `src/storage.ts` |
| 更换本地存储为 API | `src/data.ts`、`src/App.tsx` |
| 增加正式路由 | `src/App.tsx`，并新增路由配置 |
| 修改端口或 Vite 行为 | `vite.config.ts` |

## 13.1 修改数据时的注意事项

1. 新增字段必须在 `normalizeState` 里补默认值，否则读取旧数据会得到 `undefined`。
2. 删除或改名已有字段时，在 `normalizeState` 中把旧字段迁移到新字段，而不是直接丢弃。
3. 调整备份结构时提升 `BACKUP_VERSION`；`importState` 需要兼容旧版本文件。
4. 提升 `Database` 版本号时（`indexedDB.open(DB_NAME, 2)`）要在 `onupgradeneeded` 中写好迁移。
5. 任何字段调整后，用旧备份 JSON 走一遍「导入备份」，确认页面不崩。

## 14. 完成标准

项目进入可公开部署状态前，至少应满足：

| 标准 | 当前状态 |
| --- | --- |
| 正式服务端鉴权替代前端口令 | ❌ 仍是前端常量口令 |
| 数据库存储替代浏览器单机内容 | ❌ 仍是 IndexedDB |
| 图片对象存储和上传限制 | ⏳ 前端格式/体积/尺寸限制与压缩已完成，缺服务端对象存储 |
| 游客内容可备份与迁移 | ✅ JSON 导出/导入（含覆盖确认）已完成 |
| 留言跨访客同步且具备审核、防滥用机制 | ❌ 仍是单浏览器数据 |
| 生产环境 HTTPS、域名和备份完成 | ❌ 未部署 |
| 关键表单、数据仓库和游客路径有自动化测试 | ⏳ 已用 Playwright 脚本回归，缺仓库内单元测试 |
| 桌面与手机浏览器完成发布前验收 | ⏳ 桌面 + 390px 自动化已过，需真实设备复核 |
| 个人真实内容、隐私说明和外部链接已由站点所有者确认 | ❌ 等待用户录入 |

